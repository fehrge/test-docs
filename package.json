{
  "private": true,
  "name": "spinupwp-rest-api-docs",
  "main": "convert.js",
  "scripts": {
    "build": "npm run convert && npx reslate build",
    "debug": "npx reslate debug",
    "serve": "npx reslate serve",
    "convert": "node convert.js",
    "watch": "nodemon --watch openapi.yaml --watch templates -e dot,det"
  },
  "devDependencies": {
    "reslate": "^3.0.0-4",
    "widdershins": "^4.0.1"
  }
}
